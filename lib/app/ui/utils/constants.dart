import 'package:flutter/material.dart';

const padding = EdgeInsets.only(
  top: 20,
  bottom: 20,
  right: 40,
  left: 40,
);

const textButtonPadding = EdgeInsets.only(
  top: 4,
  bottom: 4,
  left: 8,
  right: 8,
);
